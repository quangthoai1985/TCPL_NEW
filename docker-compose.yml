version: '3'

services:
  gotenberg:
    image: gotenberg/gotenberg:8
    ports:
      - "8989:3000"
    environment:
      - GOTENBERG_LOG_LEVEL=info
    # Optional: Volume for persistent data if needed, but Gotenberg is mostly stateless
    # volumes:
    #   - ./gotenberg_data:/data
